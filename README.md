# AI_BASIC
This repository is for proving and rechecking about basic Ai

## 기본 설치
중간 중간 변경될 수 있음
```
conda create -n ai_basic python=3.12 -y
pip install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu118
pip install python-opencv
```

## 사용법
각 session 별로 들어가면 run.py가 있음 이거 보고 설치하기